interface:
  display_name: "Agent Prompt"
  short_description: "Repo-specific patterns and workflows for agent-prompt"
  default_prompt: "Use the agent-prompt repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true